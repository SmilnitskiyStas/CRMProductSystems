# Mosquitto config for ShelfGuard dev (TASK-062).
# Dev only: anonymous access allowed. Production must switch to
# password_file / per-device credentials before exposing port 1883.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
