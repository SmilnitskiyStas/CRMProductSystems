# ── Node / Frontend ──────────────────────────────────────────────────────────
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo
.env.local
.env*.local

# Real environment files (only the .example variants are tracked)
.env.staging
.env.production

# ── .NET / Backend ───────────────────────────────────────────────────────────
bin/
obj/
*.user
.vs/
*.suo
*.userosscache
*.sln.docstates
appsettings.Production.json
appsettings.Secrets.json

# Throwaway build/test output dirs used by agents to verify without disturbing
# the primary bin/obj (which another session's dev server may hold locked).
.build-check*/
.test-check*/
.obj-check*/
.refactor-obj*/
.catalog-obj*/
*.stackdump

# ── Docker / Infrastructure ──────────────────────────────────────────────────
docker-compose.override.yml

# ── Misc ─────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/settings.json

# Private credentials - NEVER commit
.claude/private/

# Local-only scratch working directories (not source code — e.g. local DB data dirs)
/PchilkaPosAnalytics/
