
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# CNG (Continuous Native Generation): native projects are generated by
# `expo prebuild` on EAS Build. Never commit them — a committed android/
# folder switches EAS to bare workflow and skips prebuild entirely.
/android
/ios

# Local env (EXPO_PUBLIC_* baked into dev bundles) — never commit real values
.env
.env.local
# Expo local state
.expo/
