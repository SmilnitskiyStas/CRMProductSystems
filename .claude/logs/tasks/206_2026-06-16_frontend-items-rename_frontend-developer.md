# TASK-206 — Frontend: catalog/products → items

**Agent:** frontend-developer
**Status:** done
**Date:** 2026-06-16
**Depends:** TASK-205 (backend rename, done)

## Summary
Updated both duplicate frontend product-catalog features to call the new `/api/items` routes directly (not relying on the legacy `/api/products` 301-redirect controller), added the new `itemType` field throughout, and surfaced it in the inventory CRUD UI.

## Changes
- `frontend/features/catalog/api/catalog.ts` — `getAll/getById/getByBarcode` now call `/api/items*` instead of `/api/products*`.
- `frontend/features/catalog/types.ts` — added `itemType: string` to `CatalogProductDto`.
- `frontend/features/inventory/api/products.ts` — `getAll/getById/create/update/delete` now call `/api/items*` instead of `/api/products*`.
- `frontend/features/inventory/types.ts` — added `itemType: string` to `Product`, and optional `itemType?: string` to `CreateProductPayload` (`UpdateProductPayload` inherits it via `extends`). Updated the stale "Matches CatalogProductDto" comment to "Matches ItemDto".
- `frontend/features/inventory/components/ProductForm.tsx` — added exported `ITEM_TYPE_OPTIONS` / `ITEM_TYPE_LABELS` (Ukrainian labels: Товар, Послуга, Запчастина, Розхідник, Сировина, Комплект), added `itemType` to the zod schema (default `"product"`), wired it into form defaults/reset/submit, and added a "Тип товару" `<select>` next to "Тип управління" in the form layout.
- `frontend/features/inventory/components/ProductsTable.tsx` — added a "Тип товару" column to the table (header, colSpan empty-state fix 10→11, body cell using `ITEM_TYPE_LABELS`), and added "Тип товару" to the detail drawer's "Основна інформація" section.

## Not changed (checked, no action needed)
- No literal string "Каталог продуктів" exists anywhere in `frontend/` (grepped) — the closest existing labels are "Каталог товарів" (inventory page `<h1>`) and "Каталог" (sidebar/users role labels), neither of which match the backlog's stated rename target. Left as-is since there's nothing to rename.
- `frontend/lib/` — grepped for `/api/products` and `CatalogProduct`/`catalog_products`; no hits.
- `frontend/app/(dashboard)/stock/page.tsx` (consumer of `features/catalog`) — doesn't reference `managementType`/`itemType` directly, nothing to update.
- Import path `"../api/products"` in `features/inventory/hooks/useProducts.ts` — this is a file path, not an API URL; left unchanged.

## Verification
- Grepped entire `frontend/` tree for literal `/api/products` — 0 remaining hits (only a `../api/products` *import path*, unrelated).
- `cd frontend && npx tsc --noEmit` → 0 errors.
- `cd frontend && npm run build` → succeeded (27/27 static pages generated, "Compiled successfully").

## Out of scope / follow-up
- The pre-existing duplication between `features/catalog/` (read-only) and `features/inventory/` (full CRUD) was not consolidated — both were updated consistently per task instructions, but they remain separate features.
- Backend `ProductsLegacyController` 301-redirect can be removed in a future cleanup task once all clients are confirmed off `/api/products` (per ADR-014 point 3, planned for end of this sprint).
- `frontend/tsconfig.tsbuildinfo` was regenerated by the build/tsc runs (1 line changed) — not staged/committed, it's a local build artifact.
